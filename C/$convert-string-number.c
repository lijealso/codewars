#include <stdio.h>
#include <string.h>
#include <stdlib.h>

char word[] = "-119";

int string_to_number(const char *src) {

    int x = atoi(src);


    return 0;
}

int main(){
    string_to_number(word);
}